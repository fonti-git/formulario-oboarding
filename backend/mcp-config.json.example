{
  "mcpServers": {
    "insforge": {
      "command": "npx",
      "args": [
        "-y",
        "@insforge/mcp@latest"
      ],
      "env": {
        "API_KEY": "$INSFORGE_API_KEY",
        "API_BASE_URL": "$INSFORGE_API_BASE_URL"
      }
    }
  }
}
